# セキュリティポリシー

## 対象バージョン

kb-app は 1.0 以前の開発段階にあります。セキュリティ修正の対象は `main` の最新
コミットのみです。過去のタグ・リリースへのバックポートは行っていません。

## 脆弱性の報告方法

**公開 Issue に書かないでください。** GitHub の非公開報告機能を使ってください。

1. リポジトリの **Security** タブを開く
2. **Report a vulnerability** を押す
3. 再現手順、影響範囲、確認した環境(OS、アプリのバージョンまたはコミット)を記入する

報告を受けたら 72 時間以内に受領を返信します。修正の見通しが立った段階で、
公開のタイミングを報告者と相談します。目安として、修正の公開までは報告から
90 日以内を目標とし、悪用が確認された場合はそれより早く動きます。

現時点で報奨金(bug bounty)の制度はありません。修正の公開時に、希望があれば
クレジットを記載します。

## 特に関心のある領域

kb-app は利用者の個人的な知識ベースを扱うローカルファーストのアプリケーション
です。以下の境界に関わる報告を重視します。

- **保管庫のファイル操作** — 保管庫の外へ書き込める経路、path traversal、
  シンボリックリンクの追従
- **MCP の操作面** — read / write / maintenance の面をまたいで、その面に許されて
  いない操作が実行できる経路
- **認証情報の取り扱い** — OS のキーチェーン(macOS Keychain、Windows Credential
  Manager、Linux Secret Service)へ預けたトークンの漏洩・平文保存
- **同期経路** — Git および Git LFS 経由での意図しないデータ送出、リモートの
  取り違え
- **sidecar バイナリの検証** — 同梱する git-lfs の署名・チェックサム検証の回避
- **AI クライアント経由の境界** — 会話に混入した指示によって、利用者が意図しない
  破壊的操作(削除・上書き・外部送信)が行われる経路

## 対象外

- 攻撃者がロック解除済みの利用者端末に物理的または管理者権限でアクセスできる
  ことを前提とした問題
- 依存パッケージの既知の脆弱性のうち、本アプリの経路から到達できないもの
  (報告は歓迎しますが、優先度は低くなります)
- 実際の影響を伴わないスキャナの出力

# kb-app 開発ガイド

## 製品の範囲

kb-app は、AI が管理するナレッジベースの統治アプリケーション。
公式 AI クライアントが会話の窓口、`kb-core` が製品のコア、
Tauri アプリケーションが薄い管理 UI を担う。
人間向けの汎用ノートエディターを再導入しない。

## 必ず守ること

- `docs/contract.md` が製品契約の正本。契約を変えるときは、この文書と
  契約を強制する `kb-core` のコード・テストを両方更新する。
  指示文を書くだけでは強制にならない。
- すべてのノートに 1〜4 個のタグを付ける。呼び出し側が `allow_new_tags` を
  明示しない限り、未知の語彙は拒否する。AI用MCPではこの引数を公開しない。
  `tag_vocabulary` と `get` で語彙の正本・運用を確認し、未指定なら
  `set_tag_vocabulary_source` でworkspace_idとnote_uidへ固定する。変更は現在版に照合し、
  題名や候補数だけで自動選択しない。既存語を優先して増殖を抑える。
  語彙の追加・統合・削除もAIが判断し、タグごとの本人確認を必須にしない（2026-09-08本人指定）。
  本人の明示的な訂正に従う。語彙の追加・説明変更・統合/改名・削除は
  `plan_tag_vocabulary_change`で影響を確認し、同じreceiptを`apply_tag_vocabulary_change`へ渡す。
  個別の本人承認待ちにせず、古いplanや使用中の削除・保護ノートのblockerを解消する。
  保存済みの変更を戻す場合は`plan_tag_vocabulary_rollback`→`rollback_tag_vocabulary_change`を使い、
  元実行IDの履歴に付く`rollback`を確認する。対象の変更・出力待ち・保護条件を迂回しない。
  `get_tag_vocabulary_stats`で適用・復元・出力待ちの事実を集計できるが、未計測の判断品質や
  失敗率を件数から推測しない。
  運用本文の修正は通常updateで行い、登録を再確認した後、通常ノートで使う。
- OKF frontmatter はアプリが管理する。vault 内のファイルを直接編集せず、
  コア API を使う。
- 通常ノートは AI が管理し、保存を人間の承諾待ちにしない。一時的な扱いはタグで表す。
  2026-09-06の本人依頼による提案チケットには、専用のレビュー・採否・履歴と一覧を持つ。
  この進行状態をauthorityのrole/statusへ流用せず、採否操作はMCPへ公開しない。
- 同期とインデックスは派生状態。設計上継続できる処理は障害時も継続するが、
  機能低下を隠さず利用者へ伝える。
- `app/src/lib/bindings.ts` は tauri-specta の生成物。
  Rust のソースを変更して再生成し、生成ファイルを手で編集しない。

## コーディング規約

`docs/coding-guidelines.md` に、ツールでは強制できない規約をまとめている
（コメント、ロジックの配置、エラーの階層、国際化、依存関係の採用基準）。
単純な修正を超えるコードを書く前に読む。同文書の「machine-enforced」表に
ある規則は fmt/clippy/eslint/tsc/CI で検査済みなので、ここに重複して書かない。

## リポジトリでの作業手順

- 編集前に `git status` を確認し、今回の作業と関係のないユーザーや Claude の
  変更を保持する。
- 依頼された範囲内の kb-app の変更は、工程ごとの確認を挟まず通常の引き渡しまで
  進める。対象のパスだけをステージし、必要なローカル検証後にコミットする。
  **2026-09-06 本人指定: CI費用を抑えるため、push・PR作成/更新は変更をまとめて行う。**
  コミットや小さな変更のたびにはリモートへ反映せず、本人がまとめて反映するよう
  指示した時点で、蓄積した変更を検証して作業ブランチをpushし、非draft PRを作成・更新する。
  反映後はその最終コミットのCI完了を確認する。未反映の変更はローカルにあると報告する。
  この包括的な許可には、マージ、破壊的な履歴の書き換え、無関係な外部への
  書き込み、大幅な作業範囲の拡張は含まれない。
- ローカルの `main` が最新とは限らない。レビューでは適切な upstream の参照と
  作業内容を比較する。
- 通常の MCP 接続にはインストール済みアプリの実行ファイルを使い、read・write・
  maintenance を別々に登録する。macOS では通常
  `/Applications/kb-app.app/Contents/MacOS/kb-app` を使う。
  アプリの接続操作は、自身の実行ファイルと選択中の vault を使う。
  既存クライアントが古い `target/release/kb` の登録から移行済みとは考えない。
- **稼働中の MCP バイナリはソースの変更に追従しない。** `kb-core` を変更したら
  検証し、`npm --prefix app run update:app` でアプリを配備して、
  利用中の各 AI クライアントで MCP を再接続する。
  CLI を使う開発用接続が明示的に設定されている場合は、登録先のチェックアウトで
  `cargo build --release -p kb-cli` を実行してから再接続する。
  CLI のビルドだけでは、インストール済みアプリや稼働中の MCP プロセスは更新されない。
  2026-08-14 には古いビルドが vault の `.gitattributes` から Git LFS の行を
  通知なく削除し、新しい clone で初めて問題が表面化した。
- 通常の作業では `cargo clean` を避ける。開発用 MCP の登録先がまだ
  `target/release` を指している可能性がある。どうしても必要なら先に伝え、
  対象のバイナリを再ビルドしてからクライアントを再接続する。
- リポジトリのルートには Node パッケージがない。フロントエンドのコマンドは
  `npm --prefix app` で実行する。

## 検証

コード変更の引き渡し前に、関係する検査を実行する。
層をまたぐ変更やリリースに関わる変更では、以下をすべて実行する。

```bash
npm --prefix app run check
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

ビルドとローカルアプリのコマンド:

```bash
cargo build --release
npm --prefix app run tauri dev
npm --prefix app run update:app
```

`update:app` はリリースビルド、アドホック署名、バンドルの厳密な検証を行う。
GUI だけを終了し、バンドル外に復元用のコピーを確保してアプリの内容を入れ替え、
LaunchServices への登録と起動を検証する。`kb-app --mcp` のプロセスは終了しないので、
更新後は利用中の各 AI クライアントで MCP を再接続する。
`install:app` は互換用の別名として残している。

GUI は常駐する（ADR-0017）。ウィンドウを閉じても非表示になるだけなので、
画面に見えないことは停止を意味しない。トレイメニュー、または配備スクリプトと同じ
`osascript -e 'quit app "kb-app"'` で終了する。
リリースビルドは初回起動時にログイン項目を登録する。デバッグビルドでは登録しない。

既存の製品方針と異なる設計を選ぶ前に、kb-app MCP の `search` と `get` で
過去の決定を確認する。vault 内のファイルを代替経路にしない。

KB ノート `notes/kb-app-用語と正本の地図-2026-08-13-時点` は、アプリの用語と
各ルールの正本（契約 / 要件 / ADR / KB）の対応、および内部用語と UI 用語の
使い分けをまとめている。判断の根拠となる文書や画面に出す言葉に迷ったら読む。
